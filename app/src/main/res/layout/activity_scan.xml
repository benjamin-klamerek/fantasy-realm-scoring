<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".screen.ScanActivity">

    <Button
            android:id="@+id/cameraCaptureButton"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_marginBottom="50dp"
            android:scaleType="fitCenter"
            android:text="Scan"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:elevation="2dp"/>

    <TextView
            android:text="Scanning..."
            android:layout_width="112dp"
            android:layout_height="37dp" android:id="@+id/scanningLabel"
            app:layout_constraintBottom_toTopOf="@+id/scanProgressBar"
            app:layout_constraintEnd_toEndOf="@+id/cameraPreview"
            app:layout_constraintStart_toStartOf="@+id/cameraPreview"
            app:layout_constraintTop_toTopOf="@+id/cameraPreview" app:layout_constraintVertical_bias="0.85"
            android:visibility="gone" android:textAlignment="center" android:textSize="24sp" android:textStyle="bold"/>
    <ProgressBar
            style="?android:attr/progressBarStyle"
            android:layout_width="61dp"
            android:layout_height="55dp"
            android:id="@+id/scanProgressBar" app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="@+id/cameraPreview" android:indeterminate="true"
            android:visibility="gone"
    />
    <androidx.camera.view.PreviewView
            android:id="@+id/cameraPreview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:layout_editor_absoluteX="0dp" app:layout_constraintBottom_toBottomOf="parent">
    </androidx.camera.view.PreviewView>

</androidx.constraintlayout.widget.ConstraintLayout>